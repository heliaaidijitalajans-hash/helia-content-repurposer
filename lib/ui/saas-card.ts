/** Light page panels (default SaaS). */
export const lightCardClass =
  "rounded-xl border border-gray-200 bg-white p-6 shadow-sm";

/** Dark sections only (e.g. landing hero mockup). */
export const darkSectionCardClass =
  "rounded-xl border border-white/10 bg-white/10 p-6 backdrop-blur-lg transition hover:bg-white/20";

/** Destructive / danger panel on light backgrounds. */
export const lightDangerCardClass =
  "rounded-xl border border-red-200 bg-white p-6 shadow-sm";
