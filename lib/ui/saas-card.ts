/** Dark SaaS card surface — use for panels, feature tiles, and content cards. */
export const saasCardClass =
  "rounded-xl border border-white/10 bg-white/10 p-6 backdrop-blur-lg transition hover:bg-white/20";

/** Same surface with a destructive accent border (e.g. danger zone). */
export const saasDangerCardClass =
  "rounded-xl border border-red-400/25 bg-white/10 p-6 backdrop-blur-lg transition hover:bg-white/20";
