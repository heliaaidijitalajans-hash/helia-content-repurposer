/** API yanıtı ve dahili kullanım — alan adları API ile aynı (snake_case). */
export type RepurposeResult = {
  twitter_thread: string;
  instagram_carousel: string;
  hooks: string[];
  cta: string[];
};
